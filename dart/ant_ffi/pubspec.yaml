name: ant_ffi
description: Dart/Flutter bindings for the Autonomi network
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  ffi: ^2.1.0

dev_dependencies:
  ffigen: ^11.0.0
  test: ^1.24.0
  lints: ^3.0.0
