name: flutter_example
description: Autonomi file storage example app
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter
  ant_ffi:
    path: ../ant_ffi
  file_picker: ^6.0.0
  path_provider: ^2.1.0
  desktop_drop: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
